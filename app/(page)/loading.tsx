import GlobalLoading from "@/component/ui/GlobalLoading";

/** Loading UI inside dashboard shell (sidebar/topbar stay visible) */
export default function Loading() {
  return <GlobalLoading variant="content" />;
}
