import GlobalLoading from "@/component/ui/GlobalLoading";

/** Next.js root loading UI — shown during route transitions */
export default function Loading() {
  return <GlobalLoading variant="fullscreen" />;
}
